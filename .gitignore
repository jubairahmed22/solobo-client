.vercel
node_modules
# Per-developer local secrets (NextAuth OAuth credentials, local API URL
# overrides, etc.) - never meant to be committed. Was accidentally tracked
# with real Google OAuth secrets baked in; removed from tracking below.
.env.local

# Next.js build output/cache - machine-specific, huge (some webpack cache
# packs exceed GitHub's 100MB blob limit), and regenerated on every build.
# Was accidentally committed for a while; now untracked via `git rm --cached`.
.next
tsconfig.tsbuildinfo
uuuuu